import { Typography } from "@mui/material";
import TableApp from "../components/TableApp";

export default async function Home() {
  return (
    <main>
      <Typography variant="h2">Go to In Game tab</Typography>
    </main>
  );
}
